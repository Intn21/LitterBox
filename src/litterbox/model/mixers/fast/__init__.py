"""Fast mixers: Triton / fused / chunked implementations, mirroring the file
names in ``reference/``. Empty until profiling says which one is the
bottleneck — Milestone 5. Every module here is validated against its
reference twin rather than against a paper."""
