"""Variable-tracking and multi-hop tasks.

Tests whether a model can chase a chain of references across a long
context, rather than retrieve one span from it."""

from __future__ import annotations


def run_multihop(*args, **kwargs):
    """Generate variable-tracking chains and score resolution accuracy."""
    raise NotImplementedError("Milestone 2")
